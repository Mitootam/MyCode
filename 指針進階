#define _CRT_SECURE_NO_WARNINGS 1

#include <stdio.h>

int main() {
  //二級指針 : 用來指向一級指針變量所指向的地址

  char ch = 'w';
  char *p1 = &ch;
  char** p2 = &p1;

  //指針數組

	//int* arr2[6]; //存放整型指針的數組

	//int* p1[10]; //p1是指針數組
	//int(*p2)[10];//p2是數組指針,p2可以指向一個數組,該數組有10個元素,每個元素是int類型
-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------

	//char* p = "abcdef"; //把字符串首字符a的地址，賦值給了p
	//printf("%s\n", p);


	//const char* p1 = "abcdef"; //常量字符串只需要儲存一份即可
	//const char* p2 = "abcdef";

	//char arr1[] = "abcdef";
	//char arr2[] = "abcdef";

	//if (p1 == p2)
	//	printf("p1 = p2\n");
	//else
	//	printf("p1 != p2\n");

	//if (arr1 == arr2) 
	//	printf("arr1 = arr2");
	//else
	//	printf("arr1 != arr2");
	

	//int arr[10] = { 0 };
	//printf("%p\n", arr);
	//printf("%p\n", &arr[0]);
	//printf("%p\n", &arr + 1);
	//int sz = sizeof(arr);

	//printf("%d\n",sz);

	//1. 數組名通常表示為數組首元素地址
	
	//例外
	//1.sizeof(數組名) -> 這裡的數組名為例外，表示整個數組，計算的是整個數組的大小
	//2.&數組名 -> 數組名表示依然為整個數組，所以&數組名 取出的是整個數組的地址



	return 0;
}
