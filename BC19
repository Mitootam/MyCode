//輸出 且根據條件空格
#include <stdio.h>

int main() {
    int num1,num2,num3;
    scanf("%d%d%d",&num1,&num2,&num3);
    printf("%-8d%-8d%d",num1,num2,num3);
    return 0;
}
  //補充知識:
    對齊: %-xd -> 向左對齊，位數不夠，右邊自動補空格
          %xd -> 向右對齊，位數不夠，左邊自動補空格
